* currently checksums are checked via head object requests. This should be doable by list objects
  which would be a lot cheaper.
* If there are many input files probably need to have a subset displayed with a detail view
  * also refresh all and remove bad files buttons wouldn't be bad
